#!/bin/sh

volStatus=$(amixer get Master | tail -n 1 | rev | cut -d '[' -f 1 | rev | sed 's/].*//g')
volLevel=$(amixer get Master | tail -n 1 | cut -d '[' -f 2 | sed 's/%.*//g')

if [ "$volStatus" == "on" ]
then
    echo -n  $volLevel
else
    echo -n  $volLevel
fi
